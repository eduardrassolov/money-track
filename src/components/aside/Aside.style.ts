import { styled } from "styled-components";

export const StyledAside = styled.aside`
  display: flex;
  flex-direction: column;
  height: 100vh;
  background: #fff;
  font-size: 1rem;
  padding: 1rem 0;

  ul {
    margin: 0;
    padding: 0;
    gap: 5rem;
  }
`;
