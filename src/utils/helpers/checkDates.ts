// import { isToday, isWithinInterval, subDays, subMonths, subYears } from "date-fns";

// export const isDateMoreThan = (date: Date, checkValue: string) => {
//   const currentDate = new Date();

//   switch (checkValue) {
//     case "today":
//       return isToday(date);
//     case "week":
//       return isWithinInterval(date, { start: subDays(currentDate, 7), end: currentDate });
//     case "month":
//       return isWithinInterval(date, { start: subMonths(currentDate, 1), end: currentDate });
//     case "year":
//       return isWithinInterval(date, { start: subYears(currentDate, 1), end: currentDate });
//     default:
//       return false;
//   }
// };
